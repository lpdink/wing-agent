[project]
name = "wing-hooks"
version = "0.1.0"
description = "Official hook implementations for wing agent runtime"
requires-python = ">=3.12"
dependencies = [
    "wing-gateway",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["wing_hooks"]
