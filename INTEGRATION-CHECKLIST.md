# PR #86–#90 集成测试清单

- **worktree**：`/Users/abiter/ws/wing-worktree/tui-mouse-integration`（分支 `feature/tui-mouse-integration`，从 `origin/develop` 出发按 86 → 87 → 88 → 89 → 90 顺序合并）
- **被测特性**
  - **#86** `fix/revert-alternate-scroll`：回退 alternate scroll（`?1007`），恢复鼠标上报（`?1000` + `?1002` + `?1006`）；滚轮变成独立通道；跟随契约
  - **#87** `feature/tui-text-selection`：chat 区域免修饰键拖选 + 松手复制
  - **#88** `feature/tui-scrollbar`：chat 右边缘 overlay 滚动条（hover / 点击跳转 / 拖动），`?1003` 视环境开启
  - **#89** `feature/tui-composer-pointer`：输入框内点击定位光标 + 拖选复制；模态期间屏蔽
  - **#90** `feature/tui-link-open`：markdown 链接渲染成 OSC8 + 单击打开
- **本次合并额外做的交叉集成（重点看这几条，单 PR 各自测不到）**
  1. 三个鼠标手势的**分派优先级**：滚动条列 > 区域（composer / chat）
  2. **滚动条那一列不算内容**：选区映射把它夹掉（复制不会混进 `│`，高亮不会反显滚动条）
  3. **链接 × 滚动条**：点滚动条不会打开它盖住的链接；链接可见部分照常可点
  4. 统一重绘 vocabulary：`MouseOutcome`（86/87 引入）取代 88 的 `mouse_streaming`

---

## 0. 准备

- [ ] **0.1 构建 + 启动**（release 二进制我已经构建好，`target/release/wing` 可直接跑）
  ```bash
  cd /Users/abiter/ws/wing-worktree/tui-mouse-integration
  cargo build --release          # 已构建过，增量很快
  uv sync                        # 网关侧依赖（Python 代码本次一点没改）
  ./target/release/wing          # 默认形态：需要时自动起网关，然后进 TUI
  ```
  想让 TUI 用**本 worktree** 的网关（而不是你机器上已装的）：
  ```bash
  WING_GATEWAY_CMD="$(uv run which wing-gateway)" ./target/release/wing
  ```
  或者用 venv 里的 `wing`：`make install`（maturin 会把 Rust 二进制装进 workspace venv）。
  本次 5 个 PR **只动 Rust 前端**，网关保持 develop 状态即可。
- [ ] **0.2 造一份"长且花"的会话**（要有：长文本 / 中文 / emoji / 代码块 / 链接 / 工具调用），例如发：
  > 用中文写 400 字介绍这个仓库，中间放一个 python 代码块，再给一个 https://example.com 链接，然后运行 `ls -la` 并读一下 README 的前 20 行
- [ ] **0.3 环境矩阵**：普通终端（iTerm2 / Terminal.app）跑一遍；`tmux`（或 zellij）里再跑一遍关键项（鼠标上报在复用器下有降级：**没有 hover**）。回报时请注明终端 + 是否 tmux。

---

## A. 滚轮与跟随（#86）

- [ ] **A1 滚轮滚历史** — 在 chat 上滚轮：每次 3 行，**立即**重绘（不是等 16ms 帧门）。
- [ ] **A2 面板打开时滚轮仍归 chat** — 打开 `/model` 面板 / Ask 面板 / 命令候选 popup，滚轮照样滚历史；而 ↑↓ 只导航面板（不会同时滚历史）。
- [ ] **A3 跟随契约** — ① 停在底部时新输出自动跟随；② 往上滚离开底部后，新输出**不会**把你拉回底部；③ 滚回最底部 → 自动跟随恢复（继续跟着新内容）。Ctrl+Home / Ctrl+End / PageUp / PageDown 与滚轮共用同一套状态。
- [ ] **A4 底部按下 PageDown** — 已在底部时按 PageDown 不越界、且保持跟随。
- [ ] **A5 鼠标上报确实开着** — 拖选 / 滚轮 / 点击都有效即说明上报开着；退出 TUI 后终端自身的拖选、滚轮行为完全恢复（不会留下"选中不动"的后遗症）。

## B. chat 文本选择（#87）

- [ ] **B1 单行拖选复制** — 在 assistant 文字上按住拖到另一处，松手：出现过反显高亮 → toast `Copied!` → 粘贴内容 == 屏幕上被反显的文本。
- [ ] **B2 跨 cell 多行拖选** — 从一段拖到另一段（跨消息、跨工具调用块）：粘贴回到正确多行文本；每行行尾多余空格被裁掉。
- [ ] **B3 宽字符** — 中文 / emoji：不出现半个字反显；粘贴出来的中文之间没有多余空格、没有半个 emoji。
- [ ] **B4 行首内边距** — 从 chat 最左边缘起拖（user message），粘贴出的文本**不带**行首那两个空格；而内容本身的缩进要保留。
- [ ] **B5 单击不复制** — 单击（按下即松开）：无高亮、无 toast、无 `Copied!`。
- [ ] **B6 边缘自动滚动** — 拖到 chat 顶部/底部那一行不放：视图每 50ms 滚 1 行，选区跟着滚过的行扩展；到边界后停住（不疯狂滚动、不卡 UI）。
- [ ] **B7 拖选期间流式输出** — 输出还在流的时候拖选：视图**不会**被拽回底部（follow 被冻结），高亮稳定；松手时若仍在底部，跟随恢复。
- [ ] **B8 结构变化中止** — 拖到一半触发 `/clear`、`/new`、窗口 resize：高亮立即消失、不复制、无报错；再拖选仍然正常。
- [ ] **B9 焦点丢失中止** — 拖到一半切到别的窗口再回来：高亮消失，且 View 没有卡在"阅读态"（新内容该跟还是跟）。
- [ ] **B10 与原生选择共存** — 按住 Shift（macOS Option）拖选仍然走终端原生选择（覆盖 chat 与 composer 之外的地方也能用）。

## C. overlay 滚动条（#88）

- [ ] **C1 出现条件** — 内容溢出视口 → chat 最右一列出现滚动条（细线 + 手柄）；内容不溢出（短会话 / `/clear` 之后）→ 没有滚动条。
- [ ] **C2 hover 高亮** — 普通终端：鼠标移到滚动条上 → 变亮；移开 → 变暗。**tmux/zellij 下没有 hover**（`?1003` 降级），但点击 / 拖动照常。
- [ ] **C3 点击跳转** — 点轨道空白处：视图跳到该位置（手柄以点击行为中心对齐）；底部信息分隔条上的百分比 / `位置/总数` 同步更新。
- [ ] **C4 拖动（跟手）** — 按住手柄拖动：**手柄 1:1 跟着指针**（跟手优先，步长随内容比例，长会话里一次拖动就是几十行）；把指针拖出 chat 区域 → 钳制在两端继续跟随；松开 → 拖动结束、手柄停在当前位置。细粒度请用「指针停在滚动条上滚滚轮」（3 行/档）或 `Ctrl+↑/↓`（1 行）。
- [ ] **C5 滚轮优先** — 指针停在滚动条上滚轮：仍然滚 chat（滚动条不吞滚轮）；拖动中滚轮也仍归 chat。
- [ ] **C6 跟随契约** — 把滚动条拖到最底部 → 恢复自动跟随；拖到中间 → 进入阅读态，新输出不拉回。
- [ ] **C7 状态清理** — 拖动中把内容变短（`/clear`、切到短会话）→ 滚动条消失且不残留高亮/拖拽态；重新溢出时 hover 状态是干净的。
- [ ] **C8 面板打开时** — Ask 面板 / `/model` / 候选 popup 打开时，滚动条仍可点击、拖动。

## D. composer 指针（#89）

- [ ] **D1 点击定位光标** — 在输入框里点任意位置 → 光标落在该字符处（空输入 / 单行 / 多行 / 软换行都要试）；点在 `> ` 前缀左侧 → 落到行首。
- [ ] **D2 单击不复制** — 输入框里单击：只定位光标，不复制、不 toast。
- [ ] **D3 拖选复制** — 在输入框里拖选一段 → 松手复制该片段（粘贴验证）。
- [ ] **D4 软换行** — 长单行草稿（超宽自动折行）里跨折行拖选 → 复制出的文本**不含换行符**。
- [ ] **D5 真实换行** — 多行草稿（手动回车）里跨行拖选 → 复制文本**包含** `\n`。
- [ ] **D6 空区间** — 只跨换行、一个字符都没取到（如行末拖到下一空行行首）→ 不复制、无 toast。
- [ ] **D7 模态屏蔽** — 以下情况在输入框里点击 / 拖选**不生效**（不定位光标、不复制、不高亮）：Ask 面板打开时、`/model` 面板、**可见的**候选 popup（例如刚输入 `/`）、must-select 命令无候选时（例如 `/session abc`）。反过来：Ask 面板打开时 chat 的拖选与滚轮**不受影响**。
- [ ] **D8 编辑即中止** — 输入框里拖选（还没松手）时敲键盘 / 退格 / 粘贴 → 高亮立即消失，不复制。
- [ ] **D9 跨区域夹取** — 从输入框起拖，指针划到 chat 区域 → 选区仍按输入框空间夹取（不会突然变成 chat 选区）。
- [ ] **D10 两个区域互不影响** — 先拖 chat 再拖输入框（以及反过来）：各自独立，互不中止。

## E. 链接（#90）

- [ ] **E1 渲染** — markdown 链接在 iTerm2 / kitty / WezTerm 等支持 OSC8 的终端里显示为超链接（hover 有下划线 / 可右键）；tmux 里是否透传取决于配置，不支持时退化为普通文本。
- [ ] **E2 单击打开** — 单击 `https://…` 链接 → 默认浏览器打开；**不**出现 `Copied!`。
- [ ] **E3 拖拽不打开** — 在链接上拖选 → 走选区复制（或只是选中），**不**打开。拖出去又拖回原处也不打开。
- [ ] **E4 本地路径** — `[main.rs](src/main.rs)` 这类相对路径单击 → 打开文件（相对的是 **wing 启动目录**）；写成 `src/main.rs:10` 且机器上有 `code` / `subl` / `zed` 时 → 编辑器跳到第 10 行。
- [ ] **E5 失败反馈** — 链接指向不存在的文件 → `Open failed: …` toast，且不会启动任何进程。
- [ ] **E6 只有链接才可点** — 消息里的普通文本（如 `/etc/passwd`）不可点。
- [ ] **E7 toast 遮挡** — toast 盖住某行链接时，点被盖住的地方不打开（看不见的链接不可点）。
- [ ] **E8 链接 × 滚动条（集成点）** — 找一个链接被滚动条那列盖住的会话（例如满宽的一行）：① **点滚动条不会打开**它盖住的链接；② 点链接**可见的部分**（滚动条左边）仍然打开。

## F. 交叉 + 回归

- [ ] **F1 拖选划到滚动条列（集成点）** — 在 chat 里拖选，指针划到滚动条那一列：仍然是**扩选**（不会突然变成拖滚动条）；松手复制出的文本里**没有** `│`。
- [ ] **F2 最右一列不参与选区（集成点）** — 有滚动条时从中间往右拖到底：高亮停在滚动条**左边一列**（滚动条本身永远保持可见、可点）。
- [ ] **F3 无滚动条时整列可选（集成点）** — 内容不溢出的会话里拖到最右：最右一列照常被选中并复制。
- [ ] **F4 复制干净性** — 在**有滚动条**的会话里做一次跨多行的大范围拖选：粘贴出来的文本没有 `│`、没有多余空格、行数正确。
- [ ] **F5 三手势互斥（集成点）** — ① 按在滚动条上：拖的是滚动条，不启动选区、不开链接、不复制；② 在 chat 里按下：不动滚动条；③ 在输入框里按下：不动滚动条、不动 chat 选区；④ 松手后三种手势都能马上再次使用（没有卡住的 dragging 状态）。
- [ ] **F6 常规聊天回归** — 发消息、流式输出、thinking 展示、工具调用（Bash / Read / Write / Edit / Glob / Grep）、diff 视图着色、todo 面板、Ask 面板答题、`/model` 切换、`/session`、`/copy`、Ctrl+C 中断、连按两次 Ctrl+C 退出都正常。
- [ ] **F7 退出 / 重启** — 退出 TUI：终端恢复原状（原生拖选、滚轮、光标都正常）；再次启动：上手即用（不需要点一下才生效）。
- [ ] **F8 键盘兜底** — 完全不碰鼠标：PageUp/PageDown、Ctrl+↑/↓、Ctrl+Home/End 滚动；Shift 原生选择；键盘方向键在面板 / composer 里各归其位。

## G. 已知取舍（是设计选择，不算 bug）

0. **滚动条拖动是「跟手优先」的比例映射**：手柄永远 1:1 跟着指针，因此步长 = `max_scroll / 轨道行程`（长会话每指针行几十行）——这是终端的行量化决定的，不是可调参数（浏览器之所以细腻，是因为它是像素级）。要细粒度用「指针停在滚动条上滚轮」（3 行/档）或 `Ctrl+↑/↓`（1 行）。

1. **滚动条那列占的是内容的最右一列**：内容溢出时，满宽行的最后一个字符会被 `│` 盖住，且**选不到、复制不到**（滚动条不占布局宽度 = 不重排内容，代价就是这一列）。不溢出时整列正常。
2. **tmux / zellij 下没有 hover 高亮**（`?1003` 主动降级，避免指针移动事件洪流），点击 / 拖动 / 滚轮不受影响。
3. **原生拖选需要 Shift（macOS Option）**：鼠标上报接管后，不按修饰键的拖拽交给应用内选择。
4. **模态打开时输入框指针被屏蔽**（Ask 面板 / `/model` / 可见 popup / must-select 无候选）是刻意的——那几种状态下键盘正被面板占用。
5. **OSC8 依赖终端 / 复用器配置**，不支持时链接只是普通文本（点击无效）。
6. **链接与滚动条同一列时，那一列永远归滚动条**（所以被滚动条盖住的链接不可点，可见部分照常）。

---

## 回报方式

按编号回报（例如 `B7 ✗：拖选时视图被拉回底部`），注明 **终端名 + 是否 tmux/zellij**。若某项的"期望"本身和你想要的行为不一致，也请标出来——G 组之外的一切都可以改。
