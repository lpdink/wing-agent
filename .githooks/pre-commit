#!/bin/bash
# pre-commit 只做格式门禁（等价于 CI 的 ruff format --check + cargo fmt --check）。
# 单测 / lint / 类型检查不在这里跑：提交路径要保持秒级，完整验证交给 make check 与 CI。
set -e

echo "🎨 检查代码格式..."
if ! make fmt-check; then
    echo ""
    echo "❌ 代码格式检查未通过：先运行 make fmt 修复格式，再重新提交"
    exit 1
fi
echo "✅ 代码格式检查通过"
